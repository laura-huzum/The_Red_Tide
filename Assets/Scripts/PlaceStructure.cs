using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlaceStructure : MonoBehaviour {

    // the structure's prefab, added from the UI
    GameObject structure;


    // Use this for initialization
    //void Start () {
    //instantiate the structure
    //   structure = (GameObject)Instantiate(structure, Camera.main.ScreenToViewportPoint(Input.mousePosition), Quaternion.identity);
    //}

    // Update is called once per frame
    /*void Update () {

        gameObject.transform.position = Camera.main.ScreenToViewportPoint(Input.mousePosition);


    }*/

    void spawnStructure() {

    }


}
